[Unit]
Description=crashplan container
Requires=docker.service
After=docker.service

[Service]
Restart=always
ExecStart=/usr/bin/docker start -a crashplan
ExecStop=/usr/bin/docker stop -t 2 crashplan

[Install]
WantedBy=default.target
